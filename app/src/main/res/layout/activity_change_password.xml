<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="32dp"
    android:gravity="center">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Resetare Parolă"
        android:textSize="32sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Introdu email-ul și codul de confirmare primit pentru a seta o nouă parolă."
        android:textAlignment="center"
        android:textColor="#555555"
        android:layout_marginBottom="48dp"/>

    <EditText
        android:id="@+id/etEmailReset"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:background="#EEEEEE"
        android:padding="12dp"
        android:layout_marginBottom="16dp"/>

    <EditText
        android:id="@+id/etCodConfirmare"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:hint="Cod de confirmare (ex: 123456)"
        android:inputType="number"
        android:background="#EEEEEE"
        android:padding="12dp"
        android:layout_marginBottom="40dp"/>

    <Button
        android:id="@+id/btnChangePassword"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Schimbă Parola"
        android:backgroundTint="#FF9800"
        android:textSize="18sp"/>

</LinearLayout>